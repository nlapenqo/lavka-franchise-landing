# ── Служебное / ОС ──
.DS_Store
*.log

# Локальные настройки агентов (не для репо)
.claude/
.openai/

# Комплект для передачи — пересобирается скриптом, в репо не нужен
export/

# ── Внутренние материалы проекта (не для публичного репо) ──
docs/
AGENTS.md
CLAUDE.md

# QA-скриншоты и артефакты автоматизации
blocks/_shots/
.playwright-mcp/
.playwright-cli/
output/

# Внутренние исходники и бриф заказчика (оставляем только шрифты)
references/*.docx
references/*.html
references/*.txt
references/doc-media/

# Машинные артефакты уборки 14.08 — скриншоты QA и логи автоматизации.
# Лежат на диске, в репо не нужны: их можно снять заново.
archive/2026-08-14-cleanup/output/
archive/2026-08-14-cleanup/tool-artifacts/
