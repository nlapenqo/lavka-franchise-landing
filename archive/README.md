# Архив экспериментов

Здесь лежат все черновики и старые сборки. **Ничего отсюда не идёт в прод.**

Актуальное: единственный финальный файл — `../lavka-franchise-offline.html` (веб + мобилка в одном документе); исходники — `../site/` (веб) и `../concepts/mobile/e-figma.html` (мобилка).

⚠️ Файл `landing-assemblies/final.html` — историческая сборка от 22.07.2026, а не текущий финал; не путать.

## landing-assemblies/ — сборки лендинга из блоков (iframe-стек)
- `final.html` — итоговая сборка, которую заливали на GitHub Pages (её копия стала `site/index.html`)
- `v1.html`–`v5.html` — промежуточные варианты сборки
- `bold.html` — экспериментальная «смелая» сборка (ухудшила вид — отклонена)

Пути к блокам починены (`../../blocks/`) — открываются локально через `python3 -m http.server` с корня репо.

## landing-lab/ — одностраничные экспериментальные лендинги (самодостаточные)
- `clean.html` + `clean-assets/` — вариант «clean» (отсюда берём блок цифр)
- `lab-atlas.html`, `lab-pulse.html`, `lab-press.html` + `lab-assets/` — лабораторные концепты
- `lab-index.html` — галерея lab-вариантов

## 2026-08-14-cleanup/ — уборка от 14.08.2026
Промежуточные артефакты, вынесенные из корня репо (ничего не удалено — только перемещено):
- `output/` — QA-скриншоты Playwright (сравнения вариантов блока 05, скрины Figma)
- `animation-plans/` — планы анимаций 001–003 от 13.08 (статус TODO, в `site/` не реализованы; если работа по ним продолжится — вернуть из архива)
- `tool-artifacts/playwright-cli/`, `tool-artifacts/playwright-mcp/` — логи консоли и скриншоты браузерной автоматизации (бывшие `.playwright-cli/`, `.playwright-mcp/`)
- `tool-artifacts/firecrawl/` — пустая служебная папка (бывшая `.firecrawl/`)

## 2026-08-17-site-cleanup/ — уборка `site/` от 17.08.2026
Файлы, не участвующие в сборке (`tools/build-offline-site.mjs` их не трогает) и нигде не переиспользуемые как ассеты — вынесены из `site/`, ничего не удалено:
- `site/ambient-motion-preview.html` — одноразовый прототип «5 вариантов движения градиента», не часть сайта
- `site/figma-export.html` (3,5 МБ) — плоская версия для импорта в Figma, сгенерирована `tools/figma-export/build-figma-export.py`; устарела (сайт ушёл вперёд с 23.07), при необходимости пересобрать заново
- `site/blocks/05-business/drinkit.html`, `variants.html` — черновики блока «Как устроен бизнес» до текущего `final.html`

## Где остальное
- Концепт-прототипы отдельных блоков (все версии v1…v7, num-1…num-14, bold, wf) — `blocks/` (галерея: `blocks/index.html`)
- Бейдж мероприятия — `badge/`
- Исходники, ТЗ, шрифты — `references/`, `docs/`
