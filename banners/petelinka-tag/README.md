# Performance-баннер «Классическая бирка» (Петелинка) — появление + бирка дёргается

Макет: Figma `Kec4yVv3ZhKmIQZY059Vus`, фрейм `1372:20848` (960×720). Без вступления с крупной биркой:
всё появляется наложенной лесенкой, бирка прилетает качнувшись на узелке и дальше раз в 3 с спокойно покачивается; звёздочка на цене помигивает.

## Тайминг (с)
| Что | Когда |
|---|---|
| Упаковка: сдвиг 70/24 px + прозрачность | 0–0.7 |
| Логотип, −25%, 179 ₽, 239, название | 0.10–1.0, шаг ~0.08 |
| Звёздочка, зачёркивание, 0+ и юртекст | 0.54–1.1 |
| Бирка: появляется и затухающе качается −26° → +9 → −4.5 → +2 → −0.7 → 0 | 0.42–1.52 |
| Покачивание бирки: плавно −3.5° → +2.2 → −1 → +0.4 → 0 за 2.46 с, пауза | каждые 3 с с 2.0 |
| Звёздочка на цене гаснет (opacity 0, scale 0.35) и возвращается за 0.63 с | каждые 1.5 с с 1.6 |

## Устройство
- `index.html` — всё в одном файле, стили и кейфреймы внутри. Клик по баннеру — повтор.
- Бирка — кадр 230×96 с матрицей поворота 17.4° из макета. Качание — обёртки `.pivot` на весь баннер
  с `transform-origin` в узелке (555.38, 203.33): вступление и рывки на двух вложенных обёртках, углы складываются.
- Проверка узелка: `node qa-frames.mjs <url> <out> 0.5,1,2.2 --rect ".tag__knot img"` — центр узелка на всех кадрах
  должен быть (555.38, 203.33); при сдаче дрейф 0.02 px.
- Товар — прямоугольник 500.42×662.80 с поворотом 10.48°, картинка cover.

## Figma
Анимированная копия `1380:1271` (с резкими рывками) из файла удалена при раскладке новых баннеров; актуальная анимация — в HTML.
Приём для Figma: бирку вложить в пустой фрейм с центром в узелке и вращать фрейм — ось совпадает с узелком точно.
